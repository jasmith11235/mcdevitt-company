# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# hero videos (hosted on CDN, see lib/videos.ts — never commit to repo)
/public/videos/

# source collateral (large Dropbox PDFs — reference only, never commit)
/docs/

# redesign handoff source (HTML comps, originals, decks — reference only, never commit)
/public/McDevitt-HTML-Comps/
/public/McDevitt-Assets-and-Docs/

# hero carousel handoff source (raw drop — used assets are copied into /public/images/hero/)
/public/Hero-Carousel-Assets/

# client logo handoff source (raw extraction — used logos are copied into /public/logos/clients/)
/public/Brand-Logos/

# internal audit doc (reference only — never publish under /public)
/public/McDevitt-Site-Audit-Punchlist.pdf
